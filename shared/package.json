{
  "name": "@cpd/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and schemas for the Custom Product Designer platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests in shared\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
